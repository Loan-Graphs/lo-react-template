# ─────────────────────────────────────────────────────────────────────────────
# LO Websites — Environment Variables
# Copy this file to .env.local and fill in the values before running locally
# or deploying to Vercel.
# ─────────────────────────────────────────────────────────────────────────────

# ── LoanGraphs API ──────────────────────────────────────────────────────────

# Server-side API key for LoanGraphs (used in all server-side API calls)
LOANGRAPHS_API_KEY=

# Separate key used exclusively for forwarding leads (can be the same as above)
LOANGRAPHS_LEADS_API_KEY=

# The LO's slug/ID in LoanGraphs (determines which profile is loaded)
LO_ID=

# Base URL for the LoanGraphs REST API.
# Default: https://loangraphs.com  — override only if using a staging environment
NEXT_PUBLIC_LOANGRAPHS_URL=https://loangraphs.com

# ── Site Identity (used as fallback when LoanGraphs API is unreachable) ──────

# Full URL of this deployed site (used for og:image, sitemap, etc.)
NEXT_PUBLIC_SITE_URL=https://your-lo-domain.com

# Fallback LO details — only needed if LO_ID is not set or LoanGraphs is down
NEXT_PUBLIC_LO_NAME=
NEXT_PUBLIC_LO_NMLS=
NEXT_PUBLIC_LO_PHONE=
NEXT_PUBLIC_LO_EMAIL=
NEXT_PUBLIC_LO_STATES=AZ,CO,TX
NEXT_PUBLIC_APPLY_URL=

# ── Optional Integrations ────────────────────────────────────────────────────

# Anthropic API key (used by SEO article pipeline in Phase 6)
ANTHROPIC_API_KEY=

# ── Vercel / Build ───────────────────────────────────────────────────────────
# These are set automatically by Vercel and do not need to be added manually:
#   NEXT_PUBLIC_VERCEL_URL
#   VERCEL_URL
